interface:
  display_name: "Talking-Head Auto Cut"
  short_description: "Auto-cut local talking-head and oral-presentation videos with built-in review gates"
  default_prompt: "Use $video-auto-cut-human-loop when the user gives a local talking-head or oral-presentation video path and wants a final cut from that path alone, with the built-in Step1 and Step2 reviews handled by default."
policy:
  allow_implicit_invocation: true
