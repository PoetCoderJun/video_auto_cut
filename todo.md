# TODO（实现步骤与计划）

日期：2026-02-05

## Phase 0：算法链路验证（最高优先级）
- [ ] 定义 ASR 输出统一格式（与 DashScope/Qwen 对齐）
- [ ] 复用 AutoCut 的 VAD + 片段清洗逻辑到后端工具模块
- [ ] 实现 “ASR → 对齐 → 纠错 → SRT → EDL” 自动剪辑链路
- [ ] 先用规则/阈值完成自动删减（LLM 可后置）
- [ ] 用 ffmpeg/moviepy 生成成片，验证剪辑结果
- [ ] 输出主题分段 `*.topics.json` 并用于后续 UI 分块
- [ ] 质量评分（置信度/重复度/时长）用于默认保留建议

## Phase 1：前后端工作台
- [ ] 最小化 API（`/asr` `/auto-edit` `/render`）
- [ ] 极简 Web 页面：分段列表 + 勾选/撤销 + 导出
- [ ] 编辑结果落地为 EDL + 导出参数

## Phase 2：Remotion 统一渲染
- [ ] 用 Remotion 替换临时拼接方案
- [ ] 统一字幕样式与渲染配置

## Phase 3：PPT 与模板化（后续）
- [ ] 语义段聚合 + 页面结构
- [ ] PPT 模板渲染 + 画中画布局

