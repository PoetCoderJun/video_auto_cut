# Requirements TODO

这个文档用于持续记录产品需求、实现进度和后续动作，方便代理和人工协作时保持同一份事实来源。

## 使用规则
- 新需求进入时，先补到 `Backlog`。
- 开始实现时，把条目移动到 `In Progress`，并补充负责人、相关路径或备注。
- 完成并验证后，移动到 `Done`，注明完成日期和关键结果。
- 如果需求被取消或延期，在条目后补充原因，不要直接删除历史记录。

## Backlog
- 将 ASR 后的边界策略正式拆成三层：云端识别边界、供 Test delete/polish 使用的编辑边界、最终导出/预览使用的展示边界；避免把"暴露语气词和返工痕迹供 AI 删除"和"最终字幕可读性"混成同一套切句逻辑。相关路径：`video_auto_cut/asr/transcribe.py`、`video_auto_cut/asr/dashscope_filetrans.py`、`video_auto_cut/editing/auto_edit.py`、`video_auto_cut/pi_agent_runner.py`、`skills/test-agent-editing/SKILL.md`、`skills/delete/SKILL.md`、`skills/polish/SKILL.md`、`web_api/services/test.py`
- 2026-04-15 设计稿已输出：`docs/plans/2026-04-15-asr-boundary-layering.md`，明确三层边界模型、现状评估与最小改造路径。
### 第一性原理重构清单（2026-04-16 复核版）
> 说明：按你的要求，"洁癖"本身也算正式需求，因此保留了大量一致性/去冗余项；但会把“事实判断错误”与“洁癖型清理”明确分开，避免误把错误前提推进成任务。

#### A. 主路径与模块边界批次
- 已于 2026-04-16 移入 `In Progress`，按 `$ralplan` 形成批次执行计划：`.omx/plans/2026-04-16-a-batch-module-boundaries.md`（branch: `work/2026-04-16-a-batch-ralplan`）。

#### B. 后端与核心链路洁癖批次
- [ ] **B2 拉直剪辑与字幕数据模型**：统一 `auto_edit.py` 内的 SRT / dict / 文本协议 / lines / SRT 来回转换，彻底删除 `<<REMOVE>>` 旧兼容中间态，仅保留 `<remove>` 轻量标记。相关路径：`video_auto_cut/editing/auto_edit.py`、`video_auto_cut/pi_agent_runner.py`
- [ ] **B3 简化切句与 ffmpeg 封装**：评估 `dashscope_filetrans.py` 的切句状态机，以及 `rendering/cut.py` 的 `Cutter`/重复 ffmpeg 逻辑，改成更直接、可解释的实现。相关路径：`video_auto_cut/asr/dashscope_filetrans.py`、`video_auto_cut/rendering/cut.py`
- [ ] **B4 收敛 DB/Repository/Task Queue 的兼容胶水与重复工具**：处理 `_extract_column_names`、`_executescript`、重复的 `now_iso()` / `_parse_iso()` / `_row_get()`、重复列表查询函数，以及 OSS uploader 的重复构造。相关路径：`web_api/db.py`、`web_api/repository.py`、`web_api/task_queue.py`、`web_api/services/test.py`
- [ ] **B6 重新设计任务队列，但先不预设答案**：当前只有 `TASK_TYPE_TEST`，但现有租约/心跳/回收也在承担可靠性；此项保留为“重审模型”，不先写死成内存队列或极简单表。相关路径：`web_api/task_queue.py`、`web_api/services/tasks.py`

#### 2026-04-16 复核结论（不纳入执行项）
- `video_auto_cut/asr/filetrans_like.py` 不是死代码，当前仍被 `video_auto_cut/asr/transcribe.py`、`video_auto_cut/asr/dashscope_filetrans.py` 和相关测试使用。
- “把 `pi_agent_runner.py` 改回直连 `llm_client.py`” 不作为当前 backlog，因其与 2026-04-15 已落地的 PI runner + repo-local skills 主路径冲突；若未来重开，需要单独做架构决策。
- `video_auto_cut/editing/topic_segment.py` 不能直接按“只剩旧 CLI 在用”处理，因为 `web_api/services/render_web.py` 仍引用其标题规则常量；删除前必须先抽离共享规则。
- `web_frontend/components/export-frame-preview.tsx` 的 `formatAspectRatio` 不是明确错误，只是展示规则尚未统一。

## In Progress
- 2026-04-16: **B4/B2/B3/B6 执行中**（branch: `work/2026-04-16-b-batch-ralplan`）
  - [ ] **B4 收敛 DB/Repository/Task Queue 的兼容胶水与重复工具**：已完成 `repository.py` / `task_queue.py` 的时间/row helper 收口、成功作业列表扫描合并，以及 OSS uploader factory 收口；`db.py` 内 sqlite/libsql 私有 shim 仍暂留待后续是否继续处理。相关路径：`web_api/repository.py`、`web_api/task_queue.py`、`web_api/utils/persistence_helpers.py`、`web_api/services/test.py`、`web_api/services/oss_presign.py`、`video_auto_cut/asr/oss_uploader.py`、`video_auto_cut/asr/transcribe.py`
  - [ ] **B2 拉直剪辑与字幕数据模型**：按用户最新要求，全项目保留并统一到现有轻量文本契约（delete/polish 为 `【time】句子` / `【time】<remove>句子`，chapter 为 `【start-end】标题`）；不再引入额外的重 JSON/内部归一化契约，后续继续删除 `<<REMOVE>>` 旧兼容中间态，只保留 `<remove>` 这套轻量契约。相关路径：`video_auto_cut/shared/test_text_protocol.py`、`video_auto_cut/asr/transcribe_stage.py`、`web_api/utils/srt_utils.py`、`video_auto_cut/pi_agent_runner.py`
  - [ ] **B3 简化切句与 ffmpeg 封装**：已确认 `Cutter` 无运行时消费者，并先删除 `rendering/__init__.py` 的旧导出面/大类主体，仅保留 cut helper；`dashscope_filetrans.py` 的规则函数拆分仍待继续。相关路径：`video_auto_cut/rendering/cut.py`、`video_auto_cut/rendering/__init__.py`、`video_auto_cut/asr/dashscope_filetrans.py`
  - [ ] **B6 重新设计任务队列，但先不预设答案**：已起草 ADR 草案，明确 durable queue 的当前不变量与未来方向比较；后续需评审并决定是否继续落地实现。相关路径：`.omx/drafts/2026-04-16-b6-task-queue-adr.md`、`web_api/task_queue.py`、`web_api/services/tasks.py`

- 2026-04-16: **A 主路径与模块边界批次**（branch: `work/2026-04-16-a-batch-ralplan`，plan: `.omx/plans/2026-04-16-a-batch-module-boundaries.md`）
  - [ ] **A6 补齐容器内 Test 编辑运行前提**：先完成 Step 0 gate，明确容器内 PI/Test 是支持还是 fail-fast，并用单一 capability probe 收口。相关路径：`Dockerfile`、`.pi/`、`skills/`、`video_auto_cut/orchestration/pi_capability.py`
  - [ ] **A1 下沉共享工具并恢复模块边界**：先定义 shared Test 文本协议 owner，再移除 `video_auto_cut -> web_api` 反向依赖。相关路径：`video_auto_cut/shared/`、`video_auto_cut/asr/transcribe_stage.py`、`video_auto_cut/editing/auto_edit.py`、`video_auto_cut/pi_agent_runner.py`、`web_api/utils/srt_utils.py`
  - [ ] **A4 拉直 pipeline 配置与阶段参数链**：固定 core-owned options builder，再把 ASR / auto-edit / topic path typed 化。相关路径：`video_auto_cut/orchestration/pipeline_service.py`、`video_auto_cut/orchestration/full_pipeline.py`、`web_api/services/pipeline_options.py`、`video_auto_cut/asr/transcribe.py`、`video_auto_cut/editing/auto_edit.py`、`video_auto_cut/editing/topic_segment.py`
  - [ ] **A2 收口旧流程双轨**：在 shared 协议与 typed config 稳定后，删除旧 CLI / Step2 runtime 兼容和过期接口主流程表述。相关路径：`video_auto_cut/__main__.py`、`web_api/services/step2.py`、`web_api/repository.py`、`docs/web_api_interface.md`
  - [ ] **A3 按第一性原理拆解 `render_web.py` 职责**：冻结最小 render DTO，抽离 shared title rules，并删除未接入 live path 的后端 fit/rewrite helper。相关路径：`web_api/services/render_web.py`、`video_auto_cut/shared/title_rules.py`、`web_frontend/lib/remotion/*`
  - [ ] **A5 拆离 DB 启动初始化与历史迁移**：保留当前 schema init + preflight，把 legacy migration / 删表逻辑迁到独立脚本。相关路径：`web_api/db.py`、`scripts/`、`web_api/app.py`、`web_api/worker/runner.py`

- 2026-04-16: `$ralplan` 已完成 B 批次（B1-B6）共识规划并通过 Architect/Critic 审核；建议按 Wave 1（B1/B4/B5）→ Wave 2（B2/B3）→ Wave 3（B6 ADR）推进。计划档：`.omx/plans/2026-04-16-b-batch-backend-core-cleanup.md`；当前工作分支：`work/2026-04-16-b-batch-ralplan`。

## Done
- 2026-04-16: **C 前端洁癖批次完成**（branch: `chore/requirements-c-frontend-cleanup`）：删除 `uploadAudioDirectToOss` 空别名，并将 API auth transport 收口为单一 request core（显式 `authToken` 优先于 provider token）；`job-workspace.tsx` 的 active step 改为常量映射表，`formatDuration` 统一复用 `source-video-guard.ts`；`job-draft-storage.ts` 与 `overlay-controls.ts` 收口 overlay 默认值/limits/finite-aware clamp；`export-frame-preview.tsx` 的比例展示改为标准标签优先、否则 exact fallback，覆盖 `1920x1080 -> 16:9`、`544x960 -> 9:16`、`1080x1080 -> 1:1`、`3440x1440 -> 21:9`、`720x1268 -> 180:317`。验证：`cd web_frontend && npx tsc --noEmit`、`cd web_frontend && node --experimental-strip-types --test lib/job-draft-storage.test.mjs lib/remotion/export-bitrate.test.mjs lib/remotion/typography.test.mjs`、`cd web_frontend && BETTER_AUTH_SECRET='codex-local-build-secret-1234567890' npm run build`。相关路径：`web_frontend/lib/api.ts`、`web_frontend/components/home-page-client.tsx`、`web_frontend/components/job-workspace.tsx`、`web_frontend/lib/source-video-guard.ts`、`web_frontend/lib/job-draft-storage.ts`、`web_frontend/lib/remotion/overlay-controls.ts`、`web_frontend/components/export-frame-preview.tsx`
- 2026-04-16: **B5 统一重复的 LLM/JSON 辅助逻辑**：删除 `topic_segment.py` 内重复的 `_strip_code_fence` / `_json_loads` 薄包装，统一改为直接复用 `llm_client.extract_json`，保留原有 topic 领域校验与 repair 逻辑；相关测试已通过。相关路径：`video_auto_cut/editing/topic_segment.py`、`video_auto_cut/editing/llm_client.py`
- 2026-04-16: **B1 清理确认脱离主路径的 ASR 历史残留**：删除已脱离主路径的 `video_auto_cut/asr/qwen3_asr.py` 与 `scripts/qwen3_asr_transcribe.py`，并将 `docs/current_prompts_inventory.txt`、`docs/plans/2026-03-06-shared-llm-sdk-util.md` 改为 legacy/清理说明。相关路径：`video_auto_cut/asr/qwen3_asr.py`、`scripts/qwen3_asr_transcribe.py`、`docs/current_prompts_inventory.txt`、`docs/plans/2026-03-06-shared-llm-sdk-util.md`
- 2026-04-16: **D1 清理仓库残留脚本、依赖与生成物**：删除 `scripts/extract_audio.py`、`scripts/invite_admin.py`、`moviepy` 依赖和 `skills/asr-transcribe/scripts/__init__.py` 等残留，将 `asr-transcribe` 的命令入口收口到 `python -m video_auto_cut.asr.transcribe_stage`，并补充 CLI 回归测试；`__pycache__/` 继续只作为忽略的运行产物，不再作为仓库内入口文件保留。相关路径：`scripts/`、`requirements.txt`、`skills/asr-transcribe/SKILL.md`、`video_auto_cut/asr/transcribe_stage.py`、`web_api/tests/test_repo_skill_layout.py`、`web_api/tests/test_asr_transcribe_stage_cli.py`
- 2026-04-16: **D2 迁移或删除仅服务调试页的脚本**：将 `run_browser_format_matrix.py` 与 `generate_browser_format_samples.sh` 从仓库根 `scripts/` 迁到 `web_frontend/scripts/dev-format-matrix/`，并删除已无实际消费者的 `generated-format-samples/portrait_*` 产物，只保留当前调试页仍会消费的 `small_*` 样本。相关路径：`web_frontend/scripts/dev-format-matrix/`、`web_frontend/app/dev-format-matrix/page.tsx`、`web_frontend/public/generated-format-samples/`
- 2026-04-16: **D3 重写和收口文档 source of truth**：重写 `docs/web_api_interface.md` 为当前 `/test` 单流程契约文档，修正 `docs/README.md` 的失效入口，并将 `docs/current_prompts_inventory.txt` 改成轻量入口索引，明确真正 source of truth 仍是源码 / `SKILL.md` 本身。相关路径：`docs/web_api_interface.md`、`docs/README.md`、`docs/current_prompts_inventory.txt`
- 2026-04-16: **D4 精简示例配置与忽略规则**：重写 `.env.example` 为“本地最小必填 + 高级可选覆盖”结构，补回当前 LLM / PI 编辑路径实际使用的环境变量，并将根目录 `.dockerignore` 收口为当前 API/worker Dockerfile 的真实 build-context 边界；同时明确当前 root image 不包含 `.pi/` 与 `skills/`，因此不支持容器内直接跑 repo-local PI Test 编辑。相关路径：`.env.example`、`.dockerignore`、`docs/README.md`
- 2026-04-16: 基于同一份原始输入 `test_data/media/1.srt` 对 `kimi-k2.5` 做 PI Agent thinking 对比实验（底层模型 thinking 已强制关闭，仅比较 agent 层 `off` vs `high`）：`off` 总耗时 144.376 秒，`high` 总耗时 148.641 秒（+4.265 秒，约 +2.95%）；产物上 `high` 比 `off` 多删 2 行、少保留 2 行、章节数多 1 个，并在 6 行 delete 判定和 5 行 polish 文案上出现差异。实验产物与 diff 汇总已落到 `workdir/thinking_compare_kimi/`。相关路径：`workdir/thinking_compare_kimi/off/`、`workdir/thinking_compare_kimi/high/`、`workdir/thinking_compare_kimi/comparison.json`
- 2026-04-16: 按用户要求重构 `docs/requirements_todo.md` 的 backlog 结构，将 Kimi 汇总中的需求按“主路径与模块边界 / 后端与核心链路洁癖 / 前端洁癖 / 仓库卫生与文档配置”四个可执行批次重新归类，并把若干错误前提移出执行项，改为复核结论留档。相关路径：`docs/requirements_todo.md`
- 2026-04-16: 将 `vac-llm/kimi-k2.5` 的底层 thinking 强制关闭策略改为对齐阿里云 Kimi 官方文档：在实际请求 payload 中显式传 `enable_thinking: false`，并移除 `thinking` / `reasoning_effort` / `reasoning` / `chat_template_kwargs`，避免继续使用非文档式关闭参数。相关路径：`.pi/extensions/project-llm-provider.ts`、`web_api/tests/test_pi_agent_runner.py`
- 2026-04-16: 按用户要求在本地 `.env` 切回 `LLM_MODEL=kimi-k2.5` 后重新执行 `test_data/media/1.wav` 的 PI 四步剪辑，产出 `workdir/pi_runs/20260416_1wav_kimik25_rerun/`；本轮结果共 74 行字幕，其中删除 29 行、保留 45 行，生成 7 个章节，并保留了“28天续签”“费用上调”等信息用于后续对比评估。相关路径：`workdir/pi_runs/20260416_1wav_kimik25_rerun/test.summary.json`、`workdir/pi_runs/20260416_1wav_kimik25_rerun/test.summary.test.json`、`workdir/pi_runs/20260416_1wav_kimik25_rerun/test.summary.chapters.json`
- 2026-04-16: 因用户本地 `.env` 已将 `LLM_MODEL` 切换为 `qwen3.6-plus`，按最新环境重新执行 `test_data/media/1.wav` 的 PI 四步剪辑；为兼容新模型把 `< No Speech >` 归一化成 `<No Speech>` 一类占位格式，放宽了 `delete` 合同里仅针对特殊无语音占位符的文本比对，并补充回归测试。新一轮输出位于 `workdir/pi_runs/20260416_1wav_qwen36plus/`。相关路径：`video_auto_cut/pi_agent_runner.py`、`web_api/tests/test_pi_runner_contract.py`、`workdir/pi_runs/20260416_1wav_qwen36plus/test.summary.json`
- 2026-04-16: 对 `vac-llm/qwen3.6-plus` 增加项目级自定义 `streamSimple` 包装，在实际发起 OpenAI-compatible 请求前强制把 payload 改写为 `enable_thinking: false`，并移除 `reasoning_effort` / `reasoning`，确保底层模型侧始终关闭 thinking，不再依赖 PI runner 或 TUI 当前的 thinking 档位。相关路径：`.pi/extensions/project-llm-provider.ts`、`web_api/tests/test_pi_agent_runner.py`
- 2026-04-16: 按用户新要求收紧 `polish` 的 skill 约束，明确以词语级修正为主、优先修 ASR 错词/错别字/不顺用词，尽量不对语义与语序做大改；同时增强顶层 `test-agent-editing`，要求在端到端流程中发现任何实现不清楚、不完整、不够好或仍需二次修订的点时直接回到对应步骤自我修正，而不是等待用户二次指出。相关路径：`skills/polish/SKILL.md`、`skills/test-agent-editing/SKILL.md`
- 2026-04-16: 按用户请求启动本地 PI Agent，对 `test_data/media/1.wav` 重新执行完整 Test 剪辑（`asr -> delete -> polish -> chapter`），产出 `workdir/e2e_1wav_20260416/` 下的汇总 JSON、原始/最终 Test JSON、最终 Test SRT 与章节 JSON；本次结果共 74 行字幕，其中删除 42 行、保留 32 行，生成 7 个章节。相关路径：`workdir/e2e_1wav_20260416/test.summary.json`、`workdir/e2e_1wav_20260416/test.summary.raw.test.json`、`workdir/e2e_1wav_20260416/test.summary.test.json`、`workdir/e2e_1wav_20260416/test.summary.test.srt`、`workdir/e2e_1wav_20260416/test.summary.chapters.json`
- 2026-04-16: 补充本地 PI 四步剪辑的一键启动方式，新增 `scripts/run_pi_test.sh` 自动加载仓库 `.env` 与 `.pi/settings.json` 下的本地 skills，并在 `README.md` 增加直接执行 `asr -> delete -> polish -> chapter` 的命令说明。相关路径：`scripts/run_pi_test.sh`、`README.md`
- 2026-04-16: 删除 `USE_DASHSCOPE_TEMP_OSS` 与 DashScope temp OSS 全部代码路径，统一要求 DashScope Filetrans 通过自有 OSS 或现成 `asr_oss_key` 提供可访问音频，同时在依赖中显式将 `cryptography` 升到 `pyOpenSSL 24.2.1` 兼容范围 `>=41.0.5,<44`，避免本地 `oss2 -> aliyunsdkcore -> pyOpenSSL` 初始化时因旧版 `cryptography` 崩溃。相关路径：`video_auto_cut/asr/transcribe.py`、`video_auto_cut/asr/dashscope_filetrans.py`、`video_auto_cut/orchestration/pipeline_service.py`、`web_api/config.py`、`web_api/services/pipeline_options.py`、`web_api/api/routes.py`、`.env.example`、`skills/asr-transcribe/SKILL.md`、`requirements.txt`
- 2026-04-16: 继续推进去 Test/Step2 化，将后端核心单一流程语义收口为 `test`：包括 `web_api` 常量状态、任务类型、API 路由、repository 文本持久化路径、服务函数名，以及一批后端测试；同时把前端核心接口层状态/API/draft storage 也统一到 `test` 命名。相关路径：`web_api/constants.py`、`web_api/task_queue.py`、`web_api/services/tasks.py`、`web_api/services/test.py`、`web_api/services/step2.py`、`web_api/api/routes.py`、`web_api/repository.py`、`web_api/utils/srt_utils.py`、`video_auto_cut/asr/transcribe_stage.py`、`video_auto_cut/editing/auto_edit.py`、`video_auto_cut/pi_agent_runner.py`、`web_frontend/lib/api.ts`、`web_frontend/lib/workflow.ts`、`web_frontend/lib/job-status.ts`、`web_frontend/lib/job-draft-storage.ts`、`web_frontend/components/job-workspace.tsx`
- 2026-04-15: 将 `video_auto_cut.pi_agent_runner` 扩成可直接跑完整 Test 的 CLI 任务 `--task test`，支持媒体输入时自动执行 `asr -> delete -> polish -> chapter` 四步，并产出汇总 JSON、原始/最终 Test JSON、最终 Test SRT 与章节 JSON；当日对 `test_data/raw/1.MOV` 实测发现 DashScope temp OSS 上传在大文件下因 `Broken pipe` 失败，同时用同源 `test_data/media/1.wav` 成功跑通完整四步链路。相关路径：`video_auto_cut/pi_agent_runner.py`、`workdir/e2e_1wav/test.summary.json`
- 2026-04-15: Test 编辑阶段统一以 `workdir/jobs/<job_id>/test/lines_draft.json` 作为持续更新的单一字幕草稿源；任务启动即初始化该文件，随后由 ASR、delete、polish 结果持续覆盖更新，并移除前后端的 `MERGING_SHORT_LINES` 虚假阶段展示。相关路径：`web_api/services/test.py`、`web_frontend/components/job-workspace.tsx`
- 2026-04-15: 将当前 DashScope Filetrans 转录链路抽成 repo-local skill `asr-transcribe`，新增脚本入口 `skills/asr-transcribe/scripts/run_asr_transcribe.py`，并把顶层 `test-agent-editing` 工作流扩成媒体起步时先转录、再 delete / polish / chapter 的端到端链路。相关路径：`skills/asr-transcribe/SKILL.md`、`skills/asr-transcribe/scripts/run_asr_transcribe.py`、`video_auto_cut/asr/transcribe_stage.py`、`skills/test-agent-editing/SKILL.md`、`.pi/APPEND_SYSTEM.md`、`web_api/services/test.py`、`web_api/tests/test_repo_skill_layout.py`
- 2026-04-15: 将导出预览组件 `ExportFramePreview` 改成极简视频播放器式预览，仅在组件内部新增帧号、播放/暂停、时间轴拖动与导出/原片时间映射展示，不调整导出页其它布局。相关路径：`web_frontend/components/export-frame-preview.tsx`
- 2026-04-15: 将 Test 三步合同改成更轻的用户可读格式：delete / polish 输出逐行 `【time】句子` 或 `【time】<remove>句子`，chapter 输出逐行 `【start-end】标题`；程序负责解析这些轻量文本并恢复内部结构，避免继续要求模型输出过重 JSON DTO。相关路径：`video_auto_cut/pi_agent_runner.py`、`skills/delete/SKILL.md`、`skills/polish/SKILL.md`、`skills/chapter/SKILL.md`、`skills/test-agent-editing/SKILL.md`、`web_api/tests/test_pi_runner_contract.py`、`web_api/tests/test_pi_runner_end_to_end.py`、`web_api/tests/test_auto_edit_two_pass_rules.py`、`web_api/tests/test_auto_edit_e2e.py`
- 2026-04-15: 将项目级 `.pi/settings.json` 设为 `defaultThinkingLevel: off`，并在 `pi_agent_runner` 的内部 `pi` 调用中显式传 `--thinking off`，避免 `kimi-k2.5` 落回 pi-mono 默认的 medium thinking 导致字幕编辑任务明显变慢。相关路径：`.pi/settings.json`、`video_auto_cut/pi_agent_runner.py`、`web_api/tests/test_pi_agent_runner.py`
- 2026-04-15: 按最新 ASR 参数命名方案更新仓库根 `.env`，将旧 `ASR_DASHSCOPE_*` 切到 `DASHSCOPE_ASR_*` / `ASR_*`，并移除当前不准备显式配置的云端可选项。相关路径：`.env`
- 2026-04-15: 收紧 `pi_agent_runner` 的 PI 执行边界：内部任务仅允许 `read,write,ls` 工具，并将 delete / polish / chapter prompt 改为严格的单输入单输出模式，明确禁止探索仓库、读取无关文件或修改源码，降低 `pi-mono + kimi-k2.5` 在大任务上的游走和挂起风险。相关路径：`video_auto_cut/pi_agent_runner.py`、`web_api/tests/test_pi_runner_contract.py`、`web_api/tests/test_pi_runner_end_to_end.py`
- 2026-04-15: 为排查 DashScope Filetrans 大文件直传不稳定问题，先从 `test_data/raw/1.MOV` 抽取 ASR 友好的单声道 16k WAV 到 `test_data/media/1.wav`，作为后续云端 ASR 的更小输入。相关路径：`test_data/media/1.wav`
- 2026-04-15: 重构 ASR 环境变量命名，明确区分云端 DashScope 参数与本地切句参数；新增 `DASHSCOPE_ASR_*` / `ASR_*` 方案并保留旧 `ASR_DASHSCOPE_*` 兼容回退，同时将 DashScope 请求体对齐官方 `language` / `enable_itn` / `enable_words` / `text` / `channel_id` 字段。相关路径：`web_api/config.py`、`web_api/services/pipeline_options.py`、`video_auto_cut/orchestration/pipeline_service.py`、`video_auto_cut/orchestration/full_pipeline.py`、`video_auto_cut/asr/dashscope_filetrans.py`、`video_auto_cut/asr/transcribe.py`、`.env.example`、`README.md`、`web_api/tests/test_asr_env_names.py`、`web_api/tests/test_asr_split_granularity.py`、`web_api/tests/test_step2_topic_model.py`
- 2026-04-15: 按最新章节语义要求，明确 `skills/chapter/SKILL.md` 中的 `block_range` 表示保留后的连续 block 序号范围，必须逐段给出并对全部保留内容做到连续全覆盖，不能误写成原始 `line_id` 范围。相关路径：`skills/chapter/SKILL.md`
- 2026-04-15: 按 `$writing-skills` 进一步收紧 Test 本地 skills 的发现性与流程约束：改写 `delete` / `polish` / `chapter` / `test-agent-editing` 的 description 为更强症状驱动触发条件，补充关键词覆盖、routing、required sub-skills，以及顶层工作流的 shortcuts / red flags，降低 agent 误触发与偷懒走捷径风险。相关路径：`skills/delete/SKILL.md`、`skills/polish/SKILL.md`、`skills/chapter/SKILL.md`、`skills/test-agent-editing/SKILL.md`
- 2026-04-15: 按最新技能分层要求，把口播字幕清理的端到端流程从 `.pi/APPEND_SYSTEM.md` 迁到顶层 skill `skills/test-agent-editing/SKILL.md`；system prompt 只保留项目背景、质量要求和"优先读取顶层 skill"的约束，避免把剪辑流程硬编码进系统提示。相关路径：`.pi/APPEND_SYSTEM.md`、`skills/test-agent-editing/SKILL.md`、`web_api/tests/test_pi_agent_runner.py`、`web_api/tests/test_repo_skill_layout.py`
- 2026-04-15: 按最新口播返工语义要求，增强 `.pi/APPEND_SYSTEM.md` 对"说错后不停机重说、目标是尽量免去用户二次手动剪辑"的背景说明，并同步收紧 `skills/delete/SKILL.md` 的删除标准，使 delete 明确按"返工链条 / 重说补全 / 最终表达版本"而非字面相似度判断。相关路径：`.pi/APPEND_SYSTEM.md`、`skills/delete/SKILL.md`
- 2026-04-15: 将 Test PI 执行从 Python 内部 prompt/contract 逻辑切到真正的 `pi-mono` CLI bridge：后端仅启动 `pi`，由 `.pi/APPEND_SYSTEM.md` 提供项目级 system prompt、`.pi/settings.json` 自动加载仓库 `skills/`、`.pi/extensions/project-llm-provider.ts` 注册基于 `LLM_BASE_URL` / `LLM_API_KEY` 的项目 provider；删除 `video_auto_cut/editing/pi_skill_contracts.py`。相关路径：`.pi/APPEND_SYSTEM.md`、`.pi/settings.json`、`.pi/extensions/project-llm-provider.ts`、`video_auto_cut/pi_agent_runner.py`、`video_auto_cut/editing/auto_edit.py`、`web_api/services/step2.py`、`web_api/tests/test_pi_agent_runner.py`、`web_api/tests/test_pi_runner_contract.py`、`web_api/tests/test_pi_runner_end_to_end.py`、`web_api/tests/test_auto_edit_two_pass_rules.py`、`web_api/tests/test_auto_edit_e2e.py`
- 2026-04-15: 按最新 PI mono 对接要求，项目级 PI system prompt 从后端代码迁移到 `.pi/APPEND_SYSTEM.md`，并通过 `.pi/settings.json` 将仓库根 `skills/` 注册为 PI 自动发现目录；`video_auto_cut/pi_agent_runner.py` 不再显式拼接 `--skill` / `--append-system-prompt` / `--no-skills`，避免后端耦合 skills 加载逻辑。相关路径：`.pi/APPEND_SYSTEM.md`、`.pi/settings.json`、`video_auto_cut/pi_agent_runner.py`、`web_api/tests/test_pi_agent_runner.py`
- 2026-04-15: 按最新技能治理要求，将 `delete` / `polish` / `chapter` 改为 instruction-first 的 SKILL 文档，移除 `skills/*/scripts` 下的运行脚本与 strategy 实现，避免 PI agent 通过"显式跑 skill scripts"工作。相关路径：`skills/delete/SKILL.md`、`skills/polish/SKILL.md`、`skills/chapter/SKILL.md`、`video_auto_cut/pi_agent_runner.py`、`web_api/tests/test_pi_runner_end_to_end.py`、`web_api/tests/test_repo_skill_layout.py`
- 2026-03-25: 初始化需求 todo 文档，并约定由代理持续维护。
- 2026-04-14: 盘点当前仓库源码内维护的 prompts，并归档输出到 `docs/current_prompts_inventory.txt`，便于后续统一治理 prompt source of truth。相关路径：`docs/current_prompts_inventory.txt`、`video_auto_cut/editing/auto_edit.py`、`video_auto_cut/editing/pi_agent_remove.py`、`video_auto_cut/editing/pi_agent_boundary.py`、`video_auto_cut/editing/pi_agent_polish.py`、`video_auto_cut/editing/topic_segment.py`、`video_auto_cut/editing/llm_client.py`、`video_auto_cut/asr/qwen3_asr.py`、`web_api/services/render_web.py`
- 2026-04-14: 彻底删除独立 Step2，将自动分章并入 Test 的最后一个内部子阶段；`GET/PUT /test` 收口为统一文档契约，Test 编辑页在同一时间线内完成字幕和章节确认，确认后直接进入导出，不保留任何 Step2 兼容桥接。相关路径：`web_frontend/components/job-workspace.tsx`、`web_frontend/lib/api.ts`、`web_frontend/lib/job-status.ts`、`web_frontend/lib/workflow.ts`、`web_api/constants.py`、`web_api/api/routes.py`、`web_api/repository.py`、`web_api/services/test.py`、`web_api/services/render_web.py`、`web_api/services/tasks.py`
- 2026-04-14: 修复 OSS 直传确认绑定、公共端点限流、JSON 请求体大小约束和 DashScope 结果 URL scheme 校验，收敛对象串改、暴力刷接口与本地文件读取风险。相关路径：`web_api/api/routes.py`、`web_api/services/jobs.py`、`web_api/app.py`、`web_api/schemas.py`、`video_auto_cut/asr/dashscope_filetrans.py`
- 2026-04-14: 修复 `claim_next_task()` 对 Turso 瞬时错误的函数内自动重试风险，避免队列领取在提交边界重复回放；保留 worker loop 外层重试。相关路径：`web_api/task_queue.py`、`web_api/tests/test_task_queue.py`
- 2026-04-13: 补充 `README.md` 的 Quick Start，新增 `uv` / `pi` 安装方式与 `.env` 凭证加载说明，并注明当前仓库尚未切到 `uv sync` 项目结构。相关路径：`README.md`
- 2026-04-13: 修复浏览器端 `AudioData.copyTo(...f32-planar)` 兼容异常导致的上传/导出报错，补充音频链路识别与友好提示；上传前能力校验改为逐项兜底。相关路径：`web_frontend/lib/browser-audio-pipeline-error.ts`、`web_frontend/lib/upload-render-validation.ts`、`web_frontend/lib/remotion/rendering.ts`、`web_frontend/components/job-workspace.tsx`
- 2026-04-13: 按需求取消浏览器导出阶段的"静音兜底"降级；当音频编码链路不可用或不稳定时，上传前校验与导出都直接报错，不再生成无声文件。相关路径：`web_frontend/lib/browser-audio-pipeline-error.ts`、`web_frontend/lib/upload-render-validation.ts`、`web_frontend/lib/remotion/rendering.ts`、`web_frontend/components/job-workspace.tsx`
- 2026-04-13: 按需求回退 Job Workspace 两步式字幕/章节流程与对应状态轮询，恢复"确认字幕后自动生成章节"和章节页相邻拖拽调整方式。相关路径：`web_frontend/components/job-workspace.tsx`、`web_frontend/lib/job-status.ts`、`web_frontend/lib/job-status.test.mjs`、`web_api/api/routes.py`
- 2026-03-27: 删除上传前阻断式"一帧导出探测"，避免浏览器本地预检查误判导致上传被拦截；保留前端上传失败上报。相关路径：`web_frontend/components/home-page-client.tsx`、`web_frontend/components/job-workspace.tsx`、`web_frontend/lib/api.ts`
- 2026-03-29: 本机 Ghostty 显式新增 `Ctrl+V` 原样转发到终端应用，确保 Codex TUI 可直接从系统剪贴板附加图片。相关配置：`~/.config/ghostty/config`
- 2026-04-14: 按最新 `$ralplan` 将当前 Test PI 编辑链路收敛为一个干净 PI runner，并仅保留 3 个 editing skills（`delete` / `polish` / `chapter`）；移除 chunking 主设计与显式 JSON repair/fixup prompt 依赖，把完整任务 framing 收敛到统一 system prompt，并保持 runner 与 skills 解耦。相关路径：`video_auto_cut/editing/auto_edit.py`、`video_auto_cut/editing/pi_agent_remove.py`、`video_auto_cut/editing/pi_agent_polish.py`、`video_auto_cut/editing/pi_agent_boundary.py`、`video_auto_cut/editing/pi_agent_chunking.py`、`video_auto_cut/editing/pi_agent_merge.py`、`video_auto_cut/orchestration/pipeline_service.py`、`video_auto_cut/orchestration/full_pipeline.py`、`web_api/services/test.py`、`web_api/services/step2.py`、`skills/video-auto-cut-human-loop/SKILL.md`；实施 PRD 已落档：`docs/plans/2026-04-14-pi-runner-three-skills.md`
- 2026-04-14: 针对上条需求的 Architect review 补充硬约束：唯一 `run_pi_edit(...)`/canonical runner 产出编辑结果；先冻结 `delete` / `polish` / `chapter` 三个 skill 的公开合同；主路径不得再以 chunk/boundary/reconcile 为默认编排，也不得依赖显式 JSON repair/fixup；章节 invariant 应从 `web_api` 下沉到 shared domain / runner output contract。相关路径：`video_auto_cut/editing/auto_edit.py`、`video_auto_cut/editing/pi_agent_remove.py`、`video_auto_cut/editing/pi_agent_polish.py`、`video_auto_cut/editing/pi_agent_boundary.py`、`video_auto_cut/editing/pi_agent_chunking.py`、`video_auto_cut/editing/pi_agent_merge.py`、`video_auto_cut/orchestration/pipeline_service.py`、`video_auto_cut/orchestration/full_pipeline.py`、`web_api/services/test.py`、`web_api/services/step2.py`
- 2026-04-14: 按第一性原则审视 PI agent / 剪辑链路，目标是将剪辑能力解耦为 Codex 可直接运行的 canonical Test seam；`$ralplan` 已收敛为 approval-ready 方案，下一步按单一 `run_test_artifacts(...)` 入口、Python 3.10+ runtime floor、topic/import 隔离、deployment-policy 脱钩与 chapter invariant 下沉执行。相关路径：`video_auto_cut/editing/auto_edit.py`、`video_auto_cut/editing/__init__.py`、`video_auto_cut/orchestration/pipeline_service.py`、`video_auto_cut/orchestration/full_pipeline.py`、`web_api/services/test.py`、`web_api/services/step2.py`、`skills/video-auto-cut-human-loop/SKILL.md`
- 2026-03-27: 上传前导出能力校验从阻断式一帧试渲染改为 `canRenderMediaOnWeb()` 轻量能力检查，减少误判同时保留前置提示。相关路径：`web_frontend/lib/upload-render-validation.ts`、`web_frontend/components/home-page-client.tsx`、`web_frontend/components/job-workspace.tsx`
